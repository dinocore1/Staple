
namespace com.devsmart.staple.test;

extern byte* malloc(int size);
extern int printf(byte* fmt, ... );

class LinkedListStruct {
	LinkedListStruct* prev;
	LinkedListStruct* next;
	byte* data;
}

int main() {
	LinkedListStruct a;
	a.next = malloc(15);
	a.next = 0;
	a.next.prev = malloc(15);
	a.next.prev.next = malloc(15);
	return 0;
}

/*
class Rad extends Object {
	int cool;
	
	void pizza() {
		printf("hello from Rad", this);
	}
}

class Object {
	int refCount;
}
*/




