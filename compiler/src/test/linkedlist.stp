
class LinkedList {

  class Node {
    Node* next;
    Node* previous;
    Object* data;
  }

  uint size;
  Node* head;
  Node* tail;

  void add(void* obj) {
    Node newNode;
    newNode = new Node();
    newNode.data = obj;
    newNode.previous = this.head;

    this.head.next = newNode;
    this.head = newNode;
    this.size++;
  }

  void delete(void* obj){

  }
}

