
class LinkedList {
  uint size;
  LinkedListNode* head;
  LinkedListNode* tail;

  void add(void* obj) {
    this.head = new LinkedListNode();
  }
}

class LinkedListNode {
  LinkedListNode* next;
  LinkedListNode* previous;
}