int fib(int x) {
  int retval;
  if(x == 0) {
    retval = 0;
  } else if(x == 1) {
    retval = 1;
  } else {
    retval = fib(x-1) + fib(x-2);
  }
  return retval;
}


int main() {
  int x = fib(12);
  printf("fib(12) = %d", x);
  return 0;
}


extern int printf(uint8*, ...)